#!/usr/bin/env python3
"""Shim so `bin/relay-drones` works without `pip install -e .`."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from relay_drones.cli import main

if __name__ == "__main__":
    sys.exit(main())
